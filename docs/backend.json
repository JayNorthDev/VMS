
{
  "entities": {
    "Division": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Division",
      "type": "object",
      "description": "Represents a specific department or area within the Visitor Management System that visitors can check into. This entity defines the properties of each division, including its capacity and display information.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Division entity."
        },
        "englishName": {
          "type": "string",
          "description": "The official name of the division in English."
        },
        "sinhalaName": {
          "type": "string",
          "description": "The official name of the division in Sinhala."
        },
        "backgroundColorHex": {
          "type": "string",
          "description": "Hexadecimal color code used for the division's background in the user interface."
        },
        "textColorHex": {
          "type": "string",
          "description": "Hexadecimal color code used for the division's text in the user interface."
        },
        "maximumCapacity": {
          "type": "number",
          "description": "The maximum number of visitors allowed in this division at any given time."
        }
      },
      "required": [
        "id",
        "englishName",
        "sinhalaName",
        "backgroundColorHex",
        "textColorHex",
        "maximumCapacity"
      ]
    },
    "VisitorEntry": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "VisitorEntry",
      "type": "object",
      "description": "Records a single instance of a visitor checking into and potentially out of a specific division within the Visitor Management System. It captures details about the visitor and their visit duration.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the VisitorEntry entity."
        },
        "fullName": {
          "type": "string",
          "description": "The full name of the visitor as entered during check-in."
        },
        "identificationType": {
          "type": "string",
          "description": "The type of identification provided by the visitor (e.g., NIC, Passport, Driving License)."
        },
        "identificationNumber": {
          "type": "string",
          "description": "The unique identification number of the visitor."
        },
        "address": {
          "type": "string",
          "description": "The residential address of the visitor."
        },
        "divisionId": {
          "type": "string",
          "description": "Reference to the Division the visitor is checking into. (Relationship: Division 1:N VisitorEntry)"
        },
        "checkInTime": {
          "type": "string",
          "description": "The timestamp indicating when the visitor checked into the division.",
          "format": "date-time"
        },
        "status": {
          "type": "string",
          "description": "The current status of the visitor, either 'IN' (currently checked in) or 'OUT' (has checked out)."
        },
        "checkOutTime": {
          "type": "string",
          "description": "The timestamp indicating when the visitor checked out of the division. This field is present only if the visitor has checked out.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "fullName",
        "identificationType",
        "identificationNumber",
        "address",
        "divisionId",
        "checkInTime",
        "status"
      ]
    },
    "UserProfile": {
      "title": "User Profile",
      "type": "object",
      "description": "Stores application-specific user data, including roles and custom permissions for authorization.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The user's full name."
        },
        "email": {
          "type": "string",
          "description": "The user's email address.",
          "format": "email"
        },
        "role": {
          "type": "string",
          "description": "The role assigned to the user.",
          "enum": [
            "Admin",
            "Visitor Management"
          ]
        },
        "permissions": {
          "type": "array",
          "description": "A list of specific actions the user is allowed to perform.",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "name",
        "email",
        "role",
        "permissions"
      ]
    },
    "AuditLog": {
      "title": "Audit Log",
      "type": "object",
      "description": "Records a single user action for auditing purposes.",
      "properties": {
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "The time the action occurred."
        },
        "userName": {
          "type": "string",
          "description": "The name of the user who performed the action."
        },
        "action": {
          "type": "string",
          "description": "A short description of the action type (e.g., 'Visitor Check-In')."
        },
        "details": {
          "type": "string",
          "description": "A detailed description of the action."
        }
      },
      "required": ["timestamp", "userName", "action", "details"]
    },
    "IDCard": {
      "title": "ID Card",
      "type": "object",
      "description": "Represents a physical ID card used for visitor management.",
      "properties": {
        "cardId": { "type": "string" },
        "divisionId": { "type": "string" },
        "qrCodeData": { "type": "string" },
        "status": { "type": "string", "enum": ["available", "allocated", "lost"] },
        "currentVisitorId": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["cardId", "divisionId", "qrCodeData", "status", "createdAt"]
    }
  },
  "auth": {
    "providers": [
      "password"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/divisions/{divisionId}",
        "definition": {
          "entityName": "Division",
          "schema": {
            "$ref": "#/backend/entities/Division"
          },
          "description": "Stores static configuration details for each police station division.",
          "params": [
            {
              "name": "divisionId",
              "description": "The unique identifier for a specific police station division."
            }
          ]
        }
      },
      {
        "path": "/visitorEntries/{visitorEntryId}",
        "definition": {
          "entityName": "VisitorEntry",
          "schema": {
            "$ref": "#/backend/entities/VisitorEntry"
          },
          "description": "Records individual visitor check-in and check-out events.",
          "params": [
            {
              "name": "visitorEntryId",
              "description": "The unique identifier for a specific visitor entry record."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores user profiles and roles for authorization.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for a user, matching their Firebase Auth UID."
            }
          ]
        }
      },
      {
        "path": "/audit_logs/{auditLogId}",
        "definition": {
          "entityName": "AuditLog",
          "schema": {
            "$ref": "#/backend/entities/AuditLog"
          },
          "description": "Stores a chronological log of all significant user actions.",
          "params": [
            {
              "name": "auditLogId",
              "description": "The unique identifier for an audit log entry."
            }
          ]
        }
      },
      {
        "path": "/generated_id_cards/{divisionId}/cards/{cardId}",
        "definition": {
          "entityName": "IDCard",
          "schema": {
            "$ref": "#/backend/entities/IDCard"
          },
          "description": "Stores generated ID cards organized by division sub-collections.",
          "params": [
            { "name": "divisionId", "description": "The ID of the division the cards belong to." },
            { "name": "cardId", "description": "The unique card identifier." }
          ]
        }
      }
    ]
  }
}
